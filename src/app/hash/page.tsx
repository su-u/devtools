import React, { FC } from 'react';
import { Hash } from '@/app/hash/Hash';

export const metadata = {
  title: 'Dev Toolkit - ハッシュ',
};

const HashPage: FC = () => {
  return <Hash />;
};

export default HashPage;
