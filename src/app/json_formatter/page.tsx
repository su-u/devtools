import React from 'react';
import { JsonFormatter } from '@/components/pages/json_formatter/jsonFormatter';

const JsonFormatterPage: React.FC = () => {
  return <JsonFormatter />;
};

export default JsonFormatterPage;
