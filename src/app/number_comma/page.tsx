import React, { FC } from 'react';
import { NumberComma } from '@/app/number_comma/NumberComma';

export const metadata = {
  title: 'Dev Toolkit - 数値区切り',
};

const NumberCommaPage: FC = () => {
  return <NumberComma />;
};

export default NumberCommaPage;
